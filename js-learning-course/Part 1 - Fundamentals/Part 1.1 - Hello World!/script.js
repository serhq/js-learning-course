// console.log('...') - output in console
console.log('hello, world!')
// alert("...") - pop-up window on the browser
alert('hello, world!')

// Creating a variable js,
// where js means string.
let js = 'amazing'
// Condition if js equals to amazing, browser should show up the text from alert
if (js === 'amazing') alert('JavaScript is FUN!')

js = 'boring'
if (js === 'amazing') alert('JavaScript is FUN')
// Output: none, because js doesn't equal to amazing, so the result doesn't display as expected. The variable is overwritten from 'amazing' to 'boring'.

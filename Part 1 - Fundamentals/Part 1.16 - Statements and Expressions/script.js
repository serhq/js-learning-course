/*
1. Expression
Expressions always return something.
2 + 2, x * 2, etc.

2. Statement
It may or may not produce a value, but it usually controls the flow of the program.
*/

if (23 > 10) {
    const str = '23 is bigger'; // it declares the variable, but doesn't produce the value
}

const me = 'Max'; // expression
console.log(`I'm ${2037 - 1991} years old, ${me}`); // statement with expressions inside